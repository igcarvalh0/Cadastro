DATABASE_URL="postgresql+psycopg://usuario:senha@host/banco?sslmode=require"

# Assina o cookie de sessao do login. Gere um valor proprio com:
#   python -c "import secrets; print(secrets.token_hex(32))"
# Sem isso, todo reinicio do servidor derruba quem estava logado.
SECRET_KEY="troque-por-um-valor-aleatorio"
